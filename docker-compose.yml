services:
  bifrost:
    image: docker.io/maximhq/bifrost:latest
    container_name: bifrost
    restart: always
    environment:
      - APP_PORT=8080
      - DATABASE_URL=${DATABASE_URL}
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY}
    volumes:
      - ${PWD}/config.json:/app/config.json:Z
    # 1. Limit Bifrost's console logs
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "10"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
      interval: 10s
      timeout: 5s
      retries: 3

  caddy:
    image: docker.io/library/caddy:latest
    container_name: caddy
    restart: always
    ports:
      - "80:80"
    environment:
      - ADMIN_USER=${ADMIN_USER}
      - ADMIN_PASS_HASH=${ADMIN_PASS_HASH}
    volumes:
      - ${PWD}/Caddyfile:/etc/caddy/Caddyfile:Z
      - caddy_data:/data:Z
      - caddy_config:/config:Z
    # 2. Limit Caddy's console logs (startup messages, errors)
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "10"
    depends_on:
      bifrost:
        condition: service_healthy

volumes:
  caddy_data:
  caddy_config: